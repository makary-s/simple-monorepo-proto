{
  "name": "package-b",
  "version": "1.0.0",
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "tsc --build",
    "start": "node dist",
    "test": "echo test $(pwd) && npm run build && npm run start"
  }
}
